# 🧭 Uniform Resource Compactor  
*A Simple URL Shortener Application using Python and Tkinter*

---

## 📘 Overview

**Uniform Resource Compactor** is a lightweight, user-friendly GUI tool that allows users to shorten long, complex URLs into compact, shareable links.  
It utilizes the `pyshorteners` library (TinyURL service by default) and provides a modern graphical interface built with **Tkinter**.

The application ensures a smooth user experience using **multithreading** to prevent interface freezing during network operations.  
It also supports **one-click clipboard copying** of the shortened URL.

---

## 🚀 Features

✅ Clean and intuitive Tkinter-based interface  
✅ Converts long URLs into short, shareable links  
✅ Uses TinyURL (no API key required)  
✅ Thread-safe shortening process (no GUI freezing)  
✅ One-click "Copy to Clipboard" functionality  
✅ Error handling and informative message boxes  

---

## 🧩 Technologies Used

- **Python 3.x**
- **Tkinter** – for GUI  
- **pyshorteners** – for shortening URLs  
- **pyperclip** – for clipboard operations  
- **threading** – for smooth performance  

---

## ⚙️ Installation

Make sure you have Python 3.x installed.

1. Clone the repository:
   ```bash
   git clone https://github.com/<your-username>/Uniform-Resource-Compactor.git
   cd Uniform-Resource-Compactor
